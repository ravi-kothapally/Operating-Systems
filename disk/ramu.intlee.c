#include<stdio.h>
int sub(int *s,int *a,int n1,int n2)
{
	int i=0;
	for(i=0;i<n2;i++)
	{
		for(j=0;j<n1;j++)
		{
			if(a[j]==b[i])
			{
				for(k=j;k<n2&&a[k]==;k++)
				
			}
		}
	}
}
void main()
{
 char a[30],b[30];
	gets(a);
 	int j,i=0,k;
	while(a[i]!=' ')
	{
		i++;
	}
	k=i++;
	int j=0;
	while(a[i]!='\0')
	b[j++]=a[i++];
	if(k>j)
	sub(a,b,k,j);
	else
	sub(b,a,j,k);
	
}

